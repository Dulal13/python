# Creating a tuple using ()
t = (1,3,4,5)

# Printing the elements of a tuple
print(t[0])

# Cannot update the tuple value
# t[0] = 23 throws an error
tuples = (1,) # Tuple with single element
# tuples = (1) wrong way the declare tuple with single element

print(tuples)